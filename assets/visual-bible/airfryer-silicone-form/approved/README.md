# approved/

Утверждённые кадры кампаний: `<campaign_code>/<scene_id>.png` + рядом
`<scene_id>.meta.json` (prompt, revised prompt, provider, model, timestamp,
candidate id, QA-оценки).

Кадр попадает сюда только после: победа в visual QA → sequence QA approved →
подтверждение владельца. Пока пусто — генерация не запускалась.
